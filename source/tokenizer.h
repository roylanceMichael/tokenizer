// removes puncuation from the beginning and end of a string
std::string removePuncuation(std::string currentBuffer);

// processes all the filenames in a file
bool processFileInList(char fileName[]);